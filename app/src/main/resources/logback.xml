<configuration>
    <property name="LOG_DIR" value="build/logs" />
    <property name="LOGNAME" value="cluster" />

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%logger{0} %X{replicaId} %msg%n</pattern>
        </encoder>
    </appender>

     <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${LOGNAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/${LOGNAME}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
          
                <maxFileSize>10MB</maxFileSize>
                <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%logger{0} %X{replicaId} %msg%n</pattern>
            <outputPatternAsHeader>true</outputPatternAsHeader>
        </encoder>
    </appender>


    <logger name="org.total_order_broadcast">
        <appender-ref ref="console"/>
    </logger>

    <root level="info">
        <appender-ref ref="file" />
    </root>
</configuration>